export const metadata = {
  title: 'Timeline',
  description: 'This is timeline page',
}
const layout = ({children}) => {
    return (
        <>
            {children}
            
        </>
    );
};

export default layout;