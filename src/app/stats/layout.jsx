export const metadata = {
  title: 'Statistics',
  description: 'This is stats page',
}
const layout = ({children}) => {
    return (
        <>
            {children}
            
        </>
    );
};

export default layout;