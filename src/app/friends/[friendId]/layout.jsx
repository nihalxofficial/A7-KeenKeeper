export const metadata = {
  title: 'Friend Details',
  description: 'This is friend details page',
}
const layout = ({children}) => {
    return (
        <>
            {children}
            
        </>
    );
};

export default layout;